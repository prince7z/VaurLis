services:
  # Backend Service
  - type: web
    name: Vaurlis-backend
    runtime: node
    plan: free
    region: singapore
    rootDir: ./
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    buildFilter:
      paths:
        - backend/**
        - package.json
        - tsconfig.json
        - render.yaml
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: RAZORPAY_KEY_ID
        sync: false
      - key: RAZORPAY_SECRET_KEY
        sync: false
      - key: CLOUDINARY_CLOUD_NAME
        sync: false
      - key: CLOUDINARY_API_KEY
        sync: false
      - key: CLOUDINARY_API_SECRET
        sync: false
      - key: LOGTAIL_SOURCE_TOKEN
        sync: false
      - key: SENDGRID_API_KEY
        sync: false
      - key: FRONTEND_URL
        sync: false
    healthCheckPath: /

  # Frontend Service
  - type: web
    name: VaurLis-frontend
    env: static
    rootDir: ./
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: ./frontend/dist
    buildFilter:
      paths:
        - frontend/**
        - render.yaml
    envVars:
      - key: VITE_API_URL
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
